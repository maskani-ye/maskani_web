import { redirect } from "next/navigation";

/**
 * `/` لا تُصيَّر هنا: الحافة (`middleware.ts`) تعيد كتابتها إلى
 * `/market/<code>` — نسخة **مبنيّة مسبقاً** لسوق الزائر تُخدَم من التخزين.
 *
 * ⚠️ سبب هذا الالتفاف: قراءة `headers()` داخل هذه الصفحة (لكشف السوق) كانت
 * تُخرجها من التخزين نهائياً، فبلغ LCP على الجوّال 3,948 م.ث — فوق عتبة جوجل.
 * الحافة تقرأ الترويسة رخيصةً وتوجّه إلى النسخة الجاهزة.
 *
 * هذا الملفّ شبكة أمان فقط: إن عُطّلت الحافة لأي سبب، نُحوّل إلى السوق
 * الافتراضي بدل إظهار صفحة فارغة.
 */
export default function HomePage() {
  redirect("/market/ye");
}
